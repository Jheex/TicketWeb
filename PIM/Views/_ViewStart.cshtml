@* =============================
   Define o layout padrão da página
   ============================= *@

@{
    // Layout especifica qual arquivo de layout (Master Page) será usado para esta View
    // "_Layout" normalmente está localizado em Views/Shared/_Layout.cshtml
    // Ele contém a estrutura principal da página (header, footer, scripts, etc.)
    Layout = "_Layout";
}
